class ListaService {
    constructor(ListaModel) {
        this.lista = ListaModel;
    }

    // async get(id) {
    // const listas = await this.lista.find(

    // );
    // return listas;
    // }


}

module.exports = ListaService;

